import styled from 'styled-components'

import { palette } from '@/styles/theme'

export const ErrorText = styled.p`
  margin-top: 10px;
  font-size: 0.875rem;
  font-weight: 600;
  color: ${palette.chili};
`
